name: Patch support UI integration

on:
  push:
    branches:
      - release/avalonia-hardening
    paths:
      - .github/workflows/patch-support-ui.yml
      - tools/patch-support-ui.ps1

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: release/avalonia-hardening
          fetch-depth: 0

      - name: Apply exact patch
        shell: pwsh
        run: ./tools/patch-support-ui.ps1

      - name: Commit patch
        shell: pwsh
        run: |
          if (-not (git diff --quiet)) {
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add MacroEngine/ui/AppController.cs
            git commit -m "Integrate help and About windows"
            git push origin HEAD:release/avalonia-hardening
          }
